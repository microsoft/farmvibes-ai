name: helloworld
inputs:
  user_input: DataVibe
output:
  raster: Raster
parameters:
  width: 512
  height: 256
entrypoint:
  file: helloworld.py
  callback_builder: CallbackBuilder
description:
  short_description: Test op that generates an image of the Earth with countries that intersect with the
    input geometry highlighted in orange.
