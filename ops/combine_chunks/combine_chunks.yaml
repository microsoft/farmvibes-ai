name: combine_chunks
inputs:
  chunks: List[RasterChunk]
output:
  raster: Raster
parameters:
entrypoint:
  file: combine_chunks.py
  callback_builder: CallbackBuilder
version: 2
description:
  short_description: Combines series of chunks into a final raster.
