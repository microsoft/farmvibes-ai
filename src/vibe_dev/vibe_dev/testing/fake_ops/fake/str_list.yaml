name: str_list
inputs:
  user_data: FakeType
output:
  processed_data: List[DataVibe]
parameters:
  fake_param: 1
  fake_another_param:
    fake_nested: 2
    fake_nested_too: 3
entrypoint:
  file: op.py
  callback_builder: callback_builder