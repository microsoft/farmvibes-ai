name: specific_source
tasks:
  raster:
    op: raster
    op_dir: fake
  datavibe:
    op: item_item
    op_dir: fake
sources:
  input:
    - raster.user_data
    - datavibe.user_data
sinks:
  raster: raster.processed_data
  datavibe: datavibe.processed_data