name: unknown_task_params
tasks:
  parameterizable:
    op: str_list
    op_dir: fake
    parameters:
      new_param: foo
edges:
sources:
  input:
    - parameterizable.user_data
sinks:
  output: parameterizable.processed_data