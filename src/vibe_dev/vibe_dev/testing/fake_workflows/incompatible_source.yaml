name: incompatible_source
tasks:
  raster:
    op: raster
    op_dir: fake
  timeseries:
    op: timeseries
    op_dir: fake
sources:
  input:
    - raster.user_data
    - timeseries.user_data
sinks:
  raster: raster.processed_data
  datavibe: timeseries.processed_data